# Local n8n (Docker) — demo off
lead-engine.demo.enabled=${DEMO_MODE:true}
# lead-engine.n8n.webhook-url=http://localhost:5678/webhook/lead-qualify
# lead-engine.callback-base-url=http://host.docker.internal:8081
