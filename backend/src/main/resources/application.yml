server:
  port: 8081

spring:
  application:
    name: ai-lead-acceleration-engine
  datasource:
    url: ${DB_URL:jdbc:h2:mem:leadengine;DB_CLOSE_DELAY=-1;MODE=PostgreSQL}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
    driver-class-name: ${DB_DRIVER:org.h2.Driver}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true
    show-sql: false
  sql:
    init:
      mode: never

lead-engine:
  n8n:
    webhook-url: ${N8N_WEBHOOK_URL:http://localhost:5678/webhook/lead-qualify}
    connect-timeout-ms: 3000
    # LLM workflows can take longer if the webhook waits for completion
    response-timeout-ms: 120000
  callback-base-url: ${CALLBACK_BASE_URL:http://host.docker.internal:8081}
  cors:
    allowed-origins:
      - http://localhost:5173
  demo:
    # Set DEMO_MODE=false when running live n8n docker stack
    enabled: ${DEMO_MODE:true}
    delay-ms: 800

logging:
  level:
    com.leadengine: INFO
    org.springframework.web: INFO
